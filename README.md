# foddieapp
Training React

### Configurations

##### Environment file

You will need to create your `.env` file with your own configuration variables (See `.env.example`). 
```
cp .env.example .env
```
